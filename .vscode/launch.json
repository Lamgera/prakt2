{ 
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Запуск visualizer",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/visualizer.py",
      "args": [
        "--package", "L",
        "--repo-url", "${workspaceFolder}/repo.json",
        "--repo-mode", "local",
        "--output-file", "graph.svg",
        "--max-depth", "10",
        "--filter", ""
      ],
      "console": "integratedTerminal"
    }
  ]
}